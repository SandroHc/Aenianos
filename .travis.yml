cache:
  directories:
    - vendor
    - node_modules

language: php

php:
  - 7.0

env:

before_install:
  - mysql -e 'CREATE DATABASE aenianos;'
  - composer install
  - npm install
  - php artisan migrate --seed